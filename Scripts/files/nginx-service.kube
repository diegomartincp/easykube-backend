#Servicio para acceder a NGINX
kind: Service
apiVersion: v1
metadata:
  name: {{name}}-service
  namespace: default
spec:
  selector:
    app: {{name}}
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
