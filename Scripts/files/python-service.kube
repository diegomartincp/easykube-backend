apiVersion: v1
kind: Service
metadata:
  name: {{name}}-service
  namespace: default
  labels:
    app: {{name}}
spec:
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: {{port}}
    targetPort: 5000
  selector:
    app: {{name}}
